version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 8001:8001
    depends_on:
      - mongo
    environment:
      - CORS_ENABLED=true
      - MONGO_CONN_STR=mongodb://mongo:27017
  mongo:
    image: mongo
    restart: always
    ports:
      - 27017:27017
